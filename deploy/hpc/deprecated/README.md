# Deprecated HPC entry points

These scripts are retained only for rollback and historical diagnosis. They
exercise the retired fixed CPU/GPU Worker-count model and are not called by the
current control plane. New validation must target WorkerProfile, WorkerPool,
and ResourcePlanner contracts.
